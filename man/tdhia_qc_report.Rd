% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qc_report.R
\name{tdhia_qc_report}
\alias{tdhia_qc_report}
\title{Generate QC metrics and plots for TDHIA beta matrices}
\usage{
tdhia_qc_report(
  data_beta,
  output_dir_path = getwd(),
  level = c("cpg", "probe", "icr"),
  show_plots = interactive(),
  save_pdf = F,
  save_metrics = F,
  max_density_samples = 50,
  detection_pvalue_threshold = 0.2,
  outlier_z_threshold = 3,
  verbose = TRUE
)
}
\arguments{
\item{data_beta}{Named list returned by \code{tdhia_pipeline()} or by the stepwise
TDHIA workflow. Expected to contain one of:
\code{filt_probe_beta$probe_beta_df}, \code{cpg_beta$cpg_beta_df}, or
\code{icr_beta$icr_beta_df}, depending on \code{level}.}

\item{output_dir_path}{Directory where the \code{QC} subdirectory should be
created. Defaults to the current working directory.}

\item{level}{Which matrix to summarize: \code{"cpg"} (default), \code{"probe"}, or
\code{"icr"}.}

\item{show_plots}{Logical. If \code{TRUE}, draw plots in the active graphics
device, such as the RStudio Plots pane. Defaults to \code{interactive()}.}

\item{save_pdf}{Logical. If \code{TRUE}, save all QC plots to a PDF file in the
QC directory.}

\item{save_metrics}{Logical. If \code{TRUE}, save sample-level and feature-level
QC metrics as CSV files in the QC directory.}

\item{max_density_samples}{Integer maximum number of samples to overlay in
the beta-density plot. This keeps dense studies readable. Samples are taken
from the first columns after filtering all-empty samples.}

\item{detection_pvalue_threshold}{Numeric detection p-value threshold used
for optional detection p-value QC when \code{probe_pval_df} is available.}

\item{outlier_z_threshold}{Numeric absolute z-score threshold used for simple
sample outlier flags based on missingness, mean beta, SD beta, and mean sample
correlation.}

\item{verbose}{Logical. If \code{TRUE}, print progress and output locations.}
}
\value{
A list with:
\itemize{
\item \code{sample_metrics}: sample-level NA fraction, mean beta, and SD beta.
\item \code{feature_metrics}: feature-level NA fraction.
\item \code{sample_outliers}: simple sample outlier flags.
\item \code{detection_qc}: detection p-value QC tables when available.
\item \code{retention_summary}: counts retained at probe, CpG, ICR, and sample levels.
\item \code{design_score_qc}: design score retention summary when available.
\item \code{icr_coverage_qc}: ICR coverage summary when available.
\item \code{replicate_probe_qc}: replicate probe agreement summary when available.
\item \code{matrix_dimensions}: dimensions of the selected beta matrix.
\item \code{beta_range}: observed beta range.
\item \code{qc_dir}: output QC directory.
\item \code{metrics_paths}: CSV paths when \code{save_metrics = TRUE}.
\item \code{plots_path}: PDF path when \code{save_pdf = TRUE}.
}
}
\description{
Builds a simple quality-control report for beta matrices generated by the
TDHIA processing workflow. The function is intended to be run after the data
have been imported and at least the initial probe filtering has been applied.

The default \code{level = "cpg"} uses \code{data_beta$cpg_beta$cpg_beta_df}, which is
usually the most useful matrix for downstream analysis QC. Probe-level and
ICR-level reports can also be generated when those objects are present in the
pipeline output.
}
\examples{
\dontrun{
data_beta <- tdhia_pipeline(idat_dir_paths = "path/to/idats")
qc <- tdhia_qc_report(
  data_beta,
  output_dir_path = "results",
  level = "cpg"
)
}
}
