% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_beta_density.R
\name{plot_beta_density}
\alias{plot_beta_density}
\title{Plot density of beta values for a subset of samples}
\usage{
plot_beta_density(
  subdata,
  remove_na = TRUE,
  show_legend = TRUE,
  legend_position = "right"
)
}
\arguments{
\item{subdata}{A data frame containing beta values, with probes as rows and samples as columns.}

\item{remove_na}{A logical value indicating whether to remove NA values.}

\item{show_legend}{A logical value indicating whether to show the legend.}

\item{legend_position}{A string indicating the position of the legend. Options are "none", "top", "bottom", "left", and "right".}
}
\value{
A ggplot object representing the density plot.
}
\description{
Plot density of beta values for a subset of samples
}
\examples{
# Load required namespaces
requireNamespace("ggplot2", quietly = TRUE)
requireNamespace("reshape2", quietly = TRUE)
# Assuming 'subdata' is your data frame
# Create example data frame with row names as probeset_id (replace this with your actual data)
subdata <- data.frame(
  Sample1 = runif(100),
  Sample2 = runif(100),
  Sample3 = runif(100)
)
rownames(subdata) <- paste0("probe", 1:100)  # Replace "probe" with your actual prefix

# Let's say we want to plot the density and remove NA values,
# and show the legend on the bottom
density_plot <- plot_beta_density(subdata,
                                 remove_na = TRUE,
                                 show_legend = TRUE,
                                 legend_position = "right")
print(density_plot)
}
