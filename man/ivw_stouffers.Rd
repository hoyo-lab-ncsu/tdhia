% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ivw_stouffers.R
\name{ivw_stouffers}
\alias{ivw_stouffers}
\title{ivw_stouffers}
\usage{
ivw_stouffers(
  aa_output,
  adj_p_level = 0.1,
  min_cpg = 3,
  verbose = TRUE,
  top_n_icrs = NULL
)
}
\arguments{
\item{aa_output}{the output dataframe from analyze_association which has
fit the lm model for each CpG}

\item{adj_p_level}{numeric. Max ADJ_P_VAL for a CpG to be included in CpGs/ICRs of
interest. Default = 0.10}

\item{min_cpg}{minimum number of cpg sites for an icr to be included in
results (default is 3).}

\item{verbose}{boolean, when TRUE, prints progress to command line (default = TRUE).}

\item{top_n_icrs}{number of top ICRs you would like the final results to be
filtered to include. Default=NULL (all ICRs reported)}
}
\value{
test
}
\description{
Purpose: To evaluate coordinated methylation effects across CpG within each
imprinting control region (ICR) using these steps:
0. Use analyze_associaiton to fit glm model for each CpG. The output
has the Variable (CpG), Estimate, StdError, Statistic (Walds Z), P_VAL
and ADJ_P_VAL
\enumerate{
\item Identify "ICRs of interest" (at least one CpG with FDR (ADJ_P_VAL) < 0.1
within the ICR)
\item IVW: inverse-variance-weighted. Typically a meta-analysis method, here
we are treating each CpG as an individual "study"
\item Stouffer's method:
\item Calculate the effect trait (integrates IVW + Stouffer's into one test
statistic)
}
}
\author{
Kate Everly
}
