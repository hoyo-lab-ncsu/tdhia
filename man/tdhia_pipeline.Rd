% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tdhia_pipeline.R
\name{tdhia_pipeline}
\alias{tdhia_pipeline}
\title{Run the Imprintome Processing Pipeline}
\usage{
tdhia_pipeline(
  idat_dir_paths = NULL,
  overwrite_probe_cache = F,
  probe_data_cache = NULL,
  multicore = TRUE,
  idat_basenames = NULL,
  discard_unmapped_probes = TRUE,
  max_sig_pval = 0.2,
  set_failed_betas_na = FALSE,
  max_probe_fail_rate = 0.2,
  discard_failed_probes = TRUE,
  max_patient_fail_rate = 0.25,
  smooth_adj_cpgs = FALSE,
  max_icr_fail_rate = 0.2,
  db_flag = FALSE,
  merge_replicates = "pre_beta",
  enforce_req_idats = TRUE,
  min_design_score = NA,
  verbose = T
)
}
\arguments{
\item{idat_dir_paths}{Character vector of directories containing IDAT files,
passed to load_idata_to_probes() when probe data must be processed.}

\item{overwrite_probe_cache}{Logical; reprocess an input probe-data list
when TRUE. An existing cache file supplied as a path is still reused.}

\item{probe_data_cache}{NULL, a probe-data list from load_idata_to_probes(),
or an RDS path. NULL processes without saving a cache; a missing path is
populated and an existing path is read. Parent directories are created.}

\item{multicore}{Logical or core count passed to load_idata_to_probes().}

\item{idat_basenames}{Character vector of requested sample basenames.
NULL uses all available samples; supplied names also subset cached data.}

\item{discard_unmapped_probes}{Logical; retain only mapped CpG probes when
TRUE, as determined by filter_probes().}

\item{max_sig_pval}{Numeric detection p-value threshold. Measurements pass
when their detection p-value is strictly below this value.}

\item{set_failed_betas_na}{Logical; replace individual failed measurements
with NA. Probe-level and sample-level filtering still run when FALSE.}

\item{max_probe_fail_rate}{Maximum fraction of failed measurements per
probe. Probes above the threshold have all beta values set to NA.}

\item{discard_failed_probes}{Logical; remove probes whose beta values are
all missing after filtering.}

\item{max_patient_fail_rate}{Maximum failure fraction per sample, passed
to filter_probes(); failing samples have all beta values set to NA and
are removed using that helper's default discard_failed_patients setting.}

\item{smooth_adj_cpgs}{Logical; apply a centered three-CpG rolling mean
within each ICR during CpG aggregation.}

\item{max_icr_fail_rate}{Numeric threshold passed to convert_cpgs_to_icrs().
Its current implementation reports failures but does not apply that
threshold to the returned ICR table.}

\item{db_flag}{Logical; save debugging workspaces for this function and
processing helpers that receive the flag.}

\item{merge_replicates}{Replicate handling passed to the IDAT loader:
"pre_beta" (default) averages fluorescence before calculating beta,
"post_beta" averages beta values, and NULL leaves replicates separate.}

\item{enforce_req_idats}{Logical; require all requested IDAT basenames
when loading files.}

\item{min_design_score}{Minimum probe design score. NA disables this
filter; otherwise only scores strictly greater than the threshold remain.}

\item{verbose}{Logical; print pipeline progress. This flag is not forwarded
to the processing helpers, which can print their own messages.}
}
\value{
A named list with:
\itemize{
\item probe_beta: loaded or cached probe data, subset to requested samples.
\item filt_probe_beta: filtered probe data and QC summaries from
filter_probes().
\item cpg_beta: CpG beta values and metadata from convert_probes_to_cpgs().
\item icr_beta: ICR beta values and metadata from convert_cpgs_to_icrs().
\item input_args: argument values captured for reproducing the pipeline call.
}
}
\description{
Loads IDAT measurements or cached probe data, filters probes and samples,
and aggregates methylation beta values to CpG and ICR levels.
}
\details{
Filtering and both aggregation steps run even when probe data are cached.
Cached probe data are not checked against the current processing settings.
Quantile normalization is disabled in the loader and CpG conversion calls.
See the individual processing functions for filtering and aggregation
details.
}
\seealso{
\code{\link[=load_idata_to_probes]{load_idata_to_probes()}}, \code{\link[=filter_probes]{filter_probes()}},
\code{\link[=convert_probes_to_cpgs]{convert_probes_to_cpgs()}}, \code{\link[=convert_cpgs_to_icrs]{convert_cpgs_to_icrs()}}
}
