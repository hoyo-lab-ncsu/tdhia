% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/thdia_pipeline.R
\name{tdhia_pipeline}
\alias{tdhia_pipeline}
\title{tdhia_pipeline}
\usage{
tdhia_pipeline(
  idat_dir_paths = NULL,
  OVERWRITE_TEMP_DATA = F,
  probe_data_cache_path = getwd(),
  multicore = TRUE,
  idat_basenames = NULL,
  discard_unmapped_probes = TRUE,
  max_sig_pval = 0.2,
  set_failed_betas_na = FALSE,
  max_probe_fail_rate = 0.2,
  discard_failed_probes = TRUE,
  max_patient_fail_rate = 0.25,
  smooth_adj_cpgs = FALSE,
  max_icr_fail_rate = 0.2,
  db_flag = FALSE,
  merge_replicates = "pre_beta",
  enforce_req_idats = TRUE,
  min_design_score = NA
)
}
\arguments{
\item{idat_dir_paths}{a vector of strings specifying full directory paths
where IDAT files are located.}

\item{OVERWRITE_TEMP_DATA}{todo}

\item{probe_data_cache_path}{todo}

\item{multicore}{boolean, when true uses n.cores processing in sesame.}

\item{idat_basenames}{rename idat_basenames to a patient/ study id}

\item{discard_unmapped_probes}{boolean, when true, probe_ids}

\item{max_sig_pval}{single numeric between (0-1), maximum sesame signal
p-value that is allowed}

\item{set_failed_betas_na}{boolean, when true probe measurements that fail
the p-value threshold are set to NA (missing).}

\item{max_probe_fail_rate}{maximum number of measurements that a probe can
fail the p-value threshold before being removed entirely from dataset (all
values set to NA).}

\item{discard_failed_probes}{boolean when true, the probes that fail the
max_probe_fail_rate are removed entirely.}

\item{max_patient_fail_rate}{maximum number of imprintome cpg sites that fail
the max_sig_pval threshold. Any patients with a higher fraction will be
discarded.}

\item{smooth_adj_cpgs}{boolean, when true, adjacent cpg beta values within an
ICR are smoothed with a sliding window average.}

\item{max_icr_fail_rate}{maximum allowable fraction of samples that can be
missing before the entire ICR is excluded from dataset.}

\item{db_flag}{flag for saving function work space for debugging.}

\item{merge_replicates}{string with the following possible values that
determines how replicate probes are handled.
\itemize{
\item NULL: the probe replicates are not merged (default value).
\item pre_beta: replicates are merged by averaging fluorescent signal from
each channel individually, beta values are then calculated from these
averaged values (pre_beta = merging done before beta calculation).
\item post_beta: beta value is calculated before replicates are merged and beta
values are averaged between replicates (post_beta = merging done before
beta calculation).
}}

\item{enforce_req_idats}{check that all idat files requested in idat_basenames
argument are found on disk. Throws error if this is not the case.}

\item{min_design_score}{discard probes below this design score threshold.
Default: NA (don't discard any probes by design score). Design scores vary
from 0-1, with a higher value being a better designed probe.}
}
\value{
named list with all of the output from each of the pipeline steps.
}
\description{
runs standard tdhia pipeline in a single step.
}
