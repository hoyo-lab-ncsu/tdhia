% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/test_analyze_association.R
\name{imprintome_glm}
\alias{imprintome_glm}
\title{imprintome_glm}
\usage{
imprintome_glm(
  model_str,
  study_data,
  betas,
  family,
  n_p_adj = NULL,
  max_p_val = 0.05,
  impute_na = TRUE,
  n.cores = NULL,
  db_flag = FALSE,
  rm.na.R = FALSE,
  rm.na.P = FALSE,
  m_value_transform = T,
  rm.na.Pe = FALSE,
  rm.na.all = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{model_str}{model string for glm. Should use column names in study_data,
and the term "beta" to reference the beta methylation data. Beta must either
be a response or primary predictor.}

\item{study_data}{dataframe of study variables (each row is a sample).
Rownames should be patient labels that match columns in betas.}

\item{betas}{beta matrix, each row is imprintome site (cpg or ICR), each
column is a sample (patient). Column names should be patient labels that match
rownames in study_data.}

\item{family}{string for family model argument for glm(). (ex. 'binomial', 'gaussian')}

\item{n_p_adj}{number of comparisons to use for p-value adjustment. Sometimes
is this is different than the number of actual comparisons (could be more or
less). Defaults to max columns to response and predictor variables}

\item{max_p_val}{max p-value threshold, prints number of entries below this
threshold, but does not alter computation.}

\item{impute_na}{boolean flag, when TRUE missing values in R or P are
recovered with multiple imputations with the mice package.}

\item{n.cores}{number of cores used for parallel computation. Default is NULL
and set to max cores detected minus 1.}

\item{db_flag}{boolean when true export workspace to disk for debugging.}

\item{rm.na.R}{flag to remove NA values in R prior to imputation.}

\item{rm.na.P}{flag to remove NA values in P prior to imputation.}

\item{rm.na.Pe}{flag to remove NA values in Pe prior to imputation.}

\item{rm.na.all}{flag to remove all NA values across R, P, and Pe}

\item{verbose}{prints additional info to console}
}
\value{
results with fitted coefficients from the glm, sorted by p-value
}
\description{
fits a series of general linear models with parallel processing
across each CpG or ICR site of the form

R ~ P + Pe
Where R is the response variable, P is the primary predictor, and Pe are the
covariate variables. The beta matrix (either CpG or ICR level) must be
specified with the term "beta" in the model string.
}
