% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analyze_association.R
\name{analyze_association}
\alias{analyze_association}
\title{analyze_association}
\usage{
analyze_association(
  R,
  P = NULL,
  Pe = NULL,
  family,
  n_p_adj = max(c(ncol(R), ncol(P))),
  max_p_val = 0.05,
  impute_na = TRUE,
  n.cores = NULL,
  db_flag = FALSE,
  rm.na.R = FALSE,
  rm.na.P = FALSE,
  rm.na.Pe = FALSE,
  rm.na.all = FALSE,
  print_confounders = FALSE,
  verbose = TRUE,
  icr_mapping = NULL
)
}
\arguments{
\item{R}{dataframe of response variable so  patients (rows) x variables
(columns) could either be beta values for cpg or ICR sites, or
study metadata.}

\item{P}{parallelized predictor variable(s)- data frame that is patients
(rows) x variables (columns), could either be beta values for cpg or ICR
sites. For study metadata, use Pe input argument.}

\item{Pe}{extra predictor variables that are not parallelized (included in all
fitted models).}

\item{family}{string for family model argument for glm(). (ex. 'binomial', 'gaussian')}

\item{n_p_adj}{number of comparisons to use for p-value adjustment. Sometimes
is this is different than the number of actual comparisons (could be more or
less). Defaults to max columns to response and predictor variables}

\item{max_p_val}{max p-value threshold, prints number of entries below this
threshold, but does not alter computation.}

\item{impute_na}{boolean flag, when TRUE missing values in R or P are
recovered with multiple imputations with the mice package.}

\item{n.cores}{number of cores used for parallel computation. Default is NULL
and set to max cores detected minus 1.}

\item{db_flag}{boolean when true export workspace to disk for debugging.}

\item{rm.na.R}{flag to remove NA values in R prior to imputation.}

\item{rm.na.P}{flag to remove NA values in P prior to imputation.}

\item{rm.na.Pe}{flag to remove NA values in Pe prior to imputation.}

\item{rm.na.all}{TODO}

\item{print_confounders}{TODO}

\item{verbose}{TODO}

\item{icr_mapping}{TODO}
}
\value{
results with fitted coefficients from the glm, sorted by p-value
}
\description{
fits a series of general linear models with parallel processing
using dataframes of: R response variables, P predictor variables (split across
models), and Pe predictor variables (consistent across models). The model
formula takes on the form:

If R has many columns and P has one, models are parallelized over R:

R[,i] ~ P[,1] + Pe[,1] + Pe[,2] + Pe[,3] ...

If P has many variables/ columns and R has one, models are parallelized over P:

R[,1] ~ P[,i] + Pe[,1] + Pe[,2] + Pe[,3] ...

This designed that either R or P are the CpG beta values or the ICR beta
values. The code handles either case. Note that R and P cannot both be
multicolumn.
}
