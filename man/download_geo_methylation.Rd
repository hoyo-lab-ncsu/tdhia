% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download_geo_methylation.R
\name{download_geo_methylation}
\alias{download_geo_methylation}
\title{Download methylation-array IDAT files and GEO metadata}
\usage{
download_geo_methylation(
  geo_accession,
  data_dir = tools::R_user_dir("tdhia", which = "data"),
  overwrite = FALSE,
  extract_archives = TRUE,
  decompress_idats = TRUE,
  remove_archives = FALSE,
  quiet = FALSE
)
}
\arguments{
\item{geo_accession}{Character scalar. A GEO Series accession such as
\code{"GSE268075"}.}

\item{data_dir}{Character scalar. Root directory in which GEO data are
stored. Defaults to the package-specific persistent user-data directory
returned by \code{tools::R_user_dir("tdhia", "data")}.}

\item{overwrite}{Logical. If \code{TRUE}, download and recreate existing files.}

\item{extract_archives}{Logical. If \code{TRUE}, extract \code{.tar}, \code{.tar.gz}, and
\code{.tgz} archives that may contain IDAT files.}

\item{decompress_idats}{Logical. If \code{TRUE}, decompress \code{.idat.gz} files
in the IDAT directory and remove the compressed copies after successful
decompression.}

\item{remove_archives}{Logical. If \code{TRUE}, remove downloaded archives after
successful extraction. Defaults to \code{FALSE}.}

\item{quiet}{Logical. Suppress download progress messages where possible.}
}
\value{
Invisibly returns a list containing:
\describe{
\item{geo_accession}{Normalized GEO accession.}
\item{directory}{Dataset directory.}
\item{idat_files}{Paths to discovered, decompressed IDAT files.}
\item{metadata_files}{Paths to generated metadata CSV files.}
\item{supplementary_files}{Paths to downloaded GEO supplementary files.}
}
}
\description{
Downloads IDAT files associated with a GEO Series accession and creates
sample metadata CSV files from the GEO Series Matrix records. Files are
stored in a persistent user-data directory associated with tdhia.
}
\details{
Existing files are not downloaded again unless \code{overwrite = TRUE}.

GEO commonly distributes methylation-array IDAT files either as individual
\code{.idat} or \code{.idat.gz} files or inside archives such as a \code{RAW.tar} file.
This function handles both arrangements.

Metadata are obtained from the GEO Series Matrix using
\code{GEOquery::getGEO()} and written as one CSV file per platform.
}
\examples{
result <- tdhia::download_geo_methylation("GSE268075")
}
\seealso{
\code{\link[=tdhia_geo_data_dir]{tdhia_geo_data_dir()}}, \code{\link[=tdhia_clear_geo_data]{tdhia_clear_geo_data()}}
}
\author{
ChatGPT OpenAI GPT-5.6, tested by Bruce Corliss
}
